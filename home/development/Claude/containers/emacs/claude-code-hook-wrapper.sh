#!/bin/bash
# TODO: Make a tunnel back to the host for emacs connection

# Claude Code hook wrapper
# For now, this is a no-op that accepts and ignores arguments
# Future: tunnel hook calls back to host emacs

exit 0
